Bug-1:

Issue: "Orders" tabts "Active" Filtering should be removed

way to reproduce:

in the "Orders" tab there are filtering like all, active, in progress, paid, unpaid

=============

Bug-2:

Issue:

Change —  src/app/(admin)/orders/page.tsx  (FILTERS array):

here i want to keep another option named "Pendiing".

way to reproduce:

in the "Orders" tab there are filtering like all, in progress, paid, unpaid


======

Bug-3:

Issue:modal desing inperfection. like dropdown , and thign are not properly working. lout has lots of unused portion after compleing the button. i mena buttom padding seems too high. 

way to reproduce:

"Table Management" tab -> "Add Table" modla -> this modal. & under qr code geare thigns should be deleted.as well as download things should work

=============

Bug-4:

Issue:

Clicking on ROT(Revenue Over Time) scrolls up and down when trying to go left and right through the calendar. Needs to be fixed in place. Only allowing it to go right and left.

way to reproduce:

from "Reports & Analytics" tab -> "Revenue Over Time" componant has a graph.

=================

Bug-5:

Issue: Taping & untapping of "Sales per Hour (Today)" , "Tips Collection" sections to show and unshow statistics not workign perfectly

way to reproduce:

from "Reports & Analytics" tab there are two section. Reports & Analytics, Tips Collection

============

Bug-6:

Issue:
Preparing soring in the order tab


way to reproduce:

in the "Orders" tab. ther are few filering like " pening , in progreess, ready, compled, canceled". here i also want to add anotehr filertin name "Preparing"
===========

Bug-7:

Issue:

in the "setting" tab so see the "save changes button i need to go  all the way up to bottom. here it requires two scrolls down to show the save button & two scrolls up to show the top bar can we change that to make it smoother one scroll down should show the save & one scroll up would show the top bar thank you so much 

way to reproduce:

from "setting" tab Under this option "General & Brand Settings" 


===========

Bug-8:

Issue:

adjustment in workingflow or "order tab"
we would also like to fix the flow of the ✅ button 
after its clicked that means the order is accepted & its now preparing
the button changes to Mark Ready

after Mark Ready is clicked the order is ready & the button changes to Serve 
after the order is served on the table Serve is clicked & it changes to a grey unclickable “Served”
none of these buttons should open the side menu it should only be opened if the order itself is clicked 
if you need me to explain it better please let me know thanks



way to reproduce:

from "Orders" tab ther are order card . the have button workflow

================

Bug-9:

Issue:

in the "Menu Management" tab there is a batch "unavialbe" batch in green backgrodn. there shold be two type of batch here "Available" in green. & unvaible in red.

way to reproduce:

from "Menu Management" tab there are manus with batch 

===========

Bug-10:

Issue:

from manu management -> "add item" -> there is a add customization button "Add customaization" here. every customization when added should add an english and arabic version so that they can type the name and options in both languages ofc prices & other variables have to be common please



way to reproduce:

"Manu Managemen" -> "add item" -> "Add Customiazation" -> 



in this section there are few more bugs. 

10a.The box of uploading is not centered

10a. way to reprodue:
"Manu Managemen" -> "add item" -> Product Image



as well as make sure input filed/ dropdwon is working so that this modals looks good.

=========

Bug-11:

Issue: i want a button not fix intead let it go bottom of the page and give a quick scroll  to go down thre. 

We meant that in order to reach the bottom of the page the website doesn’t allow a one quick scroll to the bottom.
It locks half way through.

way to reproduce:

Settings -> General & Brand Settings -> there is a button named "Save Changes"

=============

Bug-12:

Issue:

in tablet mode when i rotatate screen. in the "Subscription & Billing" tab -> in "Choose Your Plan". thre are three plan but select button are not horizentalolly allinged. 

way to reproduce:

from "Subscription & Billing" -> "Choose Your Plan" -> each plan has selection button.



============

Bug-13:

Issue:

We need a little bit of space between the bottom of the page and the billing history or whatever the bottom of the page has in every page. For example: it gives the illusion that there are still some billing history he could scroll down for but there isn’t .

way to repdocue:

from "Bitting" tab -> "billing history table" 

===========

Bug-14:

Issue:

"Most Popular" batch brokent when resulation changes. i mena when i make screen width goes arroudn 774px the batch broken down. it shouldnt be allowed to shift to the next line when resolution changes

way to reproduce:
from "Subscription & Billing" -> "Choose Your Plan" -> each plan has selection button.
======================

Bug-15:

Issue:
button workflow adjustment. do not open the side menu when these buttons (green zone) are pressed just execute the action that also goes for mark ready & serve
only open the side menu when the order itself is clicked (red zone)

way to reproduce:

from "Orders" -> thre are order card -> each of them has tik or "x" sign

===============
Bug-16:

Issue:
also make the ✅ & ❌  button bigger & not circular to reduce misclicks


way to reproduce:

from "Orders" -> thre are order card -> each of them has  ✅ & ❌  button sign

==============

Bug-17:

Issue:Add a list of the order number under “occupied”
They could order more than once so  make the order numbers be added too
Green zone

way to reprodue:

Under "Table Management" thre are table. some of the table are occupied table. 


is that ok or should i keep ✅ & ❌ in bigger form?

======================

Bug-18:
in the billing there is a section pro paln . here the "next renewal" is overlaping with the subtitle. i think this overlapping occured in during i am decring width from arroudn 1420 px width it start overlaping with each other. make sure is responsive 

way to reproduce. 

From "Billing" tab there is a cart named "Pro plan"

================

Bug-19:

Issue:
amazing just keep aligned with “Occupied” / green lines.
Write only the order number for example 
#0039
#0041
#0045
Without the word “Order” in the occupied table

Way to reproduce.

from the "tables" tab. there are table . in accupied table there are wrting things like order.

========================

Bug 20:

in comapct width mode price section, reject button, accept button they should not overlap

way to reproduce:

from "Orders" table -> thre are order card . in order card section price section, reject button, accept button exist